# This benchmark assumes that the data has been imported already
# (The top-level snakefile may be used to accomplish this)

focal_seqs: ../filtered-focal-seqs.qza
context_seqs: ../filtered-context-seqs.qza
context_metadata: ../context-metadata.tsv

context_N: [75, 60, 45, 30, 15]
context_R: 5
focal_N: [10, 8, 6, 4, 2]
focal_R: 5
